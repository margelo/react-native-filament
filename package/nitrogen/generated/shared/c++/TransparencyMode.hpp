///
/// TransparencyMode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::RNF {

  /**
   * An enum which can be represented as a JavaScript union (TransparencyMode).
   */
  enum class TransparencyMode {
    DEFAULT      SWIFT_NAME(default) = 0,
    TWOPASSESONESIDE      SWIFT_NAME(twopassesoneside) = 1,
    TWOPASSESTWOSIDES      SWIFT_NAME(twopassestwosides) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::RNF

namespace margelo::nitro {

  using namespace margelo::nitro::RNF;

  // C++ TransparencyMode <> JS TransparencyMode (union)
  template <>
  struct JSIConverter<TransparencyMode> {
    static inline TransparencyMode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("default"): return TransparencyMode::DEFAULT;
        case hashString("twoPassesOneSide"): return TransparencyMode::TWOPASSESONESIDE;
        case hashString("twoPassesTwoSides"): return TransparencyMode::TWOPASSESTWOSIDES;
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert \"" + unionValue + "\" to enum TransparencyMode - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, TransparencyMode arg) {
      switch (arg) {
        case TransparencyMode::DEFAULT: return JSIConverter<std::string>::toJSI(runtime, "default");
        case TransparencyMode::TWOPASSESONESIDE: return JSIConverter<std::string>::toJSI(runtime, "twoPassesOneSide");
        case TransparencyMode::TWOPASSESTWOSIDES: return JSIConverter<std::string>::toJSI(runtime, "twoPassesTwoSides");
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert TransparencyMode to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("default"):
        case hashString("twoPassesOneSide"):
        case hashString("twoPassesTwoSides"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
