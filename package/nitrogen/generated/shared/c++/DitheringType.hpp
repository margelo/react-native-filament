///
/// DitheringType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::RNF {

  /**
   * An enum which can be represented as a JavaScript union (DitheringType).
   */
  enum class DitheringType {
    NONE      SWIFT_NAME(none) = 0,
    TEMPORAL      SWIFT_NAME(temporal) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::RNF

namespace margelo::nitro {

  using namespace margelo::nitro::RNF;

  // C++ DitheringType <> JS DitheringType (union)
  template <>
  struct JSIConverter<DitheringType> {
    static inline DitheringType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"): return DitheringType::NONE;
        case hashString("temporal"): return DitheringType::TEMPORAL;
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert \"" + unionValue + "\" to enum DitheringType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, DitheringType arg) {
      switch (arg) {
        case DitheringType::NONE: return JSIConverter<std::string>::toJSI(runtime, "none");
        case DitheringType::TEMPORAL: return JSIConverter<std::string>::toJSI(runtime, "temporal");
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert DitheringType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"):
        case hashString("temporal"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
