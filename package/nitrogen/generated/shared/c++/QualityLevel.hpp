///
/// QualityLevel.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::RNF {

  /**
   * An enum which can be represented as a JavaScript union (QualityLevel).
   */
  enum class QualityLevel {
    LOW      SWIFT_NAME(low) = 0,
    MEDIUM      SWIFT_NAME(medium) = 1,
    HIGH      SWIFT_NAME(high) = 2,
    ULTRA      SWIFT_NAME(ultra) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::RNF

namespace margelo::nitro {

  using namespace margelo::nitro::RNF;

  // C++ QualityLevel <> JS QualityLevel (union)
  template <>
  struct JSIConverter<QualityLevel> {
    static inline QualityLevel fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("LOW"): return QualityLevel::LOW;
        case hashString("MEDIUM"): return QualityLevel::MEDIUM;
        case hashString("HIGH"): return QualityLevel::HIGH;
        case hashString("ULTRA"): return QualityLevel::ULTRA;
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert \"" + unionValue + "\" to enum QualityLevel - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, QualityLevel arg) {
      switch (arg) {
        case QualityLevel::LOW: return JSIConverter<std::string>::toJSI(runtime, "LOW");
        case QualityLevel::MEDIUM: return JSIConverter<std::string>::toJSI(runtime, "MEDIUM");
        case QualityLevel::HIGH: return JSIConverter<std::string>::toJSI(runtime, "HIGH");
        case QualityLevel::ULTRA: return JSIConverter<std::string>::toJSI(runtime, "ULTRA");
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert QualityLevel to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("LOW"):
        case hashString("MEDIUM"):
        case hashString("HIGH"):
        case hashString("ULTRA"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
