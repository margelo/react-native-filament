///
/// CullingMode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::RNF {

  /**
   * An enum which can be represented as a JavaScript union (CullingMode).
   */
  enum class CullingMode {
    NONE      SWIFT_NAME(none) = 0,
    BACK      SWIFT_NAME(back) = 1,
    FRONT      SWIFT_NAME(front) = 2,
    FRONTANDBACK      SWIFT_NAME(frontandback) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::RNF

namespace margelo::nitro {

  using namespace margelo::nitro::RNF;

  // C++ CullingMode <> JS CullingMode (union)
  template <>
  struct JSIConverter<CullingMode> {
    static inline CullingMode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"): return CullingMode::NONE;
        case hashString("back"): return CullingMode::BACK;
        case hashString("front"): return CullingMode::FRONT;
        case hashString("frontAndBack"): return CullingMode::FRONTANDBACK;
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert \"" + unionValue + "\" to enum CullingMode - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, CullingMode arg) {
      switch (arg) {
        case CullingMode::NONE: return JSIConverter<std::string>::toJSI(runtime, "none");
        case CullingMode::BACK: return JSIConverter<std::string>::toJSI(runtime, "back");
        case CullingMode::FRONT: return JSIConverter<std::string>::toJSI(runtime, "front");
        case CullingMode::FRONTANDBACK: return JSIConverter<std::string>::toJSI(runtime, "frontAndBack");
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert CullingMode to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("none"):
        case hashString("back"):
        case hashString("front"):
        case hashString("frontAndBack"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
